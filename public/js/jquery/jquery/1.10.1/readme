
1、cmd 需要修改最后cmd判断，以暴露全局借口$,要不保找不到对象

// module.exports = jQuery;
module.exports = window.$

2、amd选择配置对于的位置路径
// define("jquery/jquery/1.10.1/jquery-debug", [], function () { return jQuery; } );
define("js/jquery/jquery/1.10.1/jquery-debug", [], function () { return jQuery; } );